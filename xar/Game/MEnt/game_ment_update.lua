--This is totally deprecated because now
--I use alarms for everything.

function p.typical(
    inst_id,
    elapsed_time,
    elapsed_level_time)
    
    --Nothing to do here.
    --Everything has been replaced with alarms.
    --It used to be that we called
    --game_ment_shoot.update,
    --and changed the EMP countdown.

    --Could add more here if I wanted,
    --but it is probably faster for the system
    --to use alarms.

    --Might want to add to here for debugging.
end
