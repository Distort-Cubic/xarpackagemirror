-- Number of Lua scripts = 202
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_0.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(0)
    local old_value = ga_get_i("xar.player.gun0.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(9) then return end    
    local max_value = game_wep_modes.get_ammo_max(0)
    local old_value = ga_get_i("xar.player.gun0.ammo")
    if ( old_value ~= max_value ) then
        ga_set_i("xar.player.gun0.ammo", max_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Dark hole gun recharged")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "Dark Holes"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_0_once.lua"
function p.__get_mesh() return "bent_ammo_gun_0" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(0)
    local old_value = ga_get_i("xar.player.gun0.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(9) then return end    
    local max_value = game_wep_modes.get_ammo_max(0)
    local old_value = ga_get_i("xar.player.gun0.ammo")
    if ( old_value ~= max_value ) then
        ga_set_i("xar.player.gun0.ammo", max_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        game_msg.add("Dark hole gun recharged")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "Dark Holes (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_1.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(1)
    local old_value = ga_get_i("xar.player.gun1.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    local max_value = game_wep_modes.get_ammo_max(1)
    local old_value = ga_get_i("xar.player.gun1.ammo")
    if( old_value < max_value ) then
        ga_set_i("xar.player.gun1.ammo", max_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Plasma gun recharged")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Plasma"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_1_once.lua"
function p.__get_mesh() return "bent_ammo_gun_1" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(1)
    local old_value = ga_get_i("xar.player.gun1.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    local max_value = game_wep_modes.get_ammo_max(1)
    local old_value = ga_get_i("xar.player.gun1.ammo")
    if( old_value < max_value ) then
        ga_set_i("xar.player.gun1.ammo", max_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        game_msg.add("Plasma gun recharged")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Plasma (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_2_huge.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 200
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        if( amount >= 100 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " shells"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Shells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_2_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + 25
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 shell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " shells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "25 Shells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_2_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + 25
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 shell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " shells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "25 Shells (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_2_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 shell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " shells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

--This is called by other scripts.
function p.cannot_use_msg()
    return "^xff0000You need an upgrade for this weapon first"
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "5 Shells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_2_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_2_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "5 Shells (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_3_huge.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 200
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(2) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(2)
    local old_value = ga_get_i("xar.player.gun2.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun2.ammo", new_value)
        if( amount >= 100 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " shells"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(2) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Shells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_3_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(3) then return end
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    local new_value = old_value + 32
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun3.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 cell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " cells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "32 Cells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_3_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_3_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(3) then return end
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    local new_value = old_value + 32
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun3.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 cell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " cells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "32 Cells (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_3_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(3) then return end
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    local new_value = old_value + 8
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun3.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 cell")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " cells")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "8 Cells"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_3_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_3_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(3) then return end
    local max_value = game_wep_modes.get_ammo_max(3)
    local old_value = ga_get_i("xar.player.gun3.ammo")
    local new_value = old_value + 8
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun3.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(3) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "8 Cells (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_4_huge.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 200
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(4) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun4.ammo", new_value)
        if( amount >= 100 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " rockets"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(4) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_4_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(4) then return end
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun4.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local cur_value = ga_get_i("xar.player.gun4.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(4) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "20 Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_4_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(4) then return end
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun4.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local cur_value = ga_get_i("xar.player.gun4.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(4) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "20 Rockets (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_4_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(4) then return end
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun4.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(4) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "5 Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_4_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_4_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(4) then return end
    local max_value = game_wep_modes.get_ammo_max(4)
    local old_value = ga_get_i("xar.player.gun4.ammo")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun4.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(4) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "5 Rockets (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_5_huge.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 2000 --Making it worse.
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(5) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun5.ammo", new_value)
        if( amount >= 100 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " homing rockets"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(5) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Homing Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_5_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(5) then return end
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    local new_value = old_value + 100
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun5.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 homing rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " homing rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local cur_value = ga_get_i("xar.player.gun5.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(5) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "100 Homing Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_5_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_5_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(5) then return end
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    local new_value = old_value + 100
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun5.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 homing rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " homing rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local cur_value = ga_get_i("xar.player.gun5.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(5) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "100 Homing Rockets (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_5_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(5) then return end
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun5.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 homing rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " homing rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    if not game_can_use.main(5) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local max_value = game_wep_modes.get_ammo_max(5)
    local cur_value = ga_get_i("xar.player.gun5.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "20 Homing Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_5_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_5_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(5) then return end
    local max_value = game_wep_modes.get_ammo_max(5)
    local old_value = ga_get_i("xar.player.gun5.ammo")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun5.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 homing rocket")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " homing rockets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(5) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "20 Homing Rockets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_6_huge.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 1000
    --default_amount = 10000
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(6) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun6.ammo", new_value)
        if( amount >= 100 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " bullets"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(6) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Bullets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_6_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(6) then return end
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun6.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 bullet")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " bullets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(6) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "50 Bullets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_6_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(6) then return end
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun6.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 bullet")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " bullets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(6) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "50 Bullets (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_6_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    return ( old_value < max_value );
end


function p.payload(level, bp)
    if not game_can_use.main(6) then return end
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    local new_value = old_value + 12
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun6.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 bullet")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " bullets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local cur_value = ga_get_i("xar.player.gun6.ammo")
    return (cur_value < max_value)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(6) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "12 Bullets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_6_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_6_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(6) then return end
    local max_value = game_wep_modes.get_ammo_max(6)
    local old_value = ga_get_i("xar.player.gun6.ammo")
    local new_value = old_value + 12
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun6.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 bullet")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " bullets")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(6) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "12 Bullets"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_7_large.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(7) then return end
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun7.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(7) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "50 Rails"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_7_large_once.lua"
function p.__get_mesh() return "bent_ammo_gun_7_large" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(7) then return end
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun7.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(7) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "50 Rails (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_7_small.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(7) then return end
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    local new_value = old_value + 10
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun7.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(7) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "10 Rails"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_7_small_once.lua"
function p.__get_mesh() return "bent_ammo_gun_7_small" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(7) then return end
    local max_value = game_wep_modes.get_ammo_max(7)
    local old_value = ga_get_i("xar.player.gun7.ammo")
    local new_value = old_value + 10
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun7.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        local num_got = new_value - old_value
        if num_got == 1 then
            game_msg.add("You got 1 rail")
        else
            game_msg.add("You got " .. tostring(new_value - old_value) .. " rails")
        end
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(7) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "10 Rails (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_8.lua"
function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(8)
    local old_value = ga_get_i("xar.player.gun8.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(8) then return end    
    local max_value = game_wep_modes.get_ammo_max(8)
    local old_value = ga_get_i("xar.player.gun8.ammo")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun8.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got an EMP")
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(8) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "EMP"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_8_once.lua"
function p.__get_mesh() return "bent_ammo_gun_8" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = game_wep_modes.get_ammo_max(8)
    local old_value = ga_get_i("xar.player.gun8.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(8) then return end    
    local max_value = game_wep_modes.get_ammo_max(8)
    local old_value = ga_get_i("xar.player.gun8.ammo")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun8.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got an EMP")
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(8) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "EMP (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_9.lua"
function p.has_effect(level, bp)
    max_value = game_wep_modes.get_ammo_max(9)
    old_value = ga_get_i("xar.player.gun9.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(9) then return end    
    local max_value = game_wep_modes.get_ammo_max(9)
    local old_value = ga_get_i("xar.player.gun9.ammo")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun9.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got a nuke")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(9) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "Nuke"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_9_huge.lua"
function p.has_effect(level, bp)
    max_value = game_wep_modes.get_ammo_max(9)
    old_value = ga_get_i("xar.player.gun9.ammo")
    return ( old_value < max_value );
end

function p.ammo(level, bp)
    local default_amount = 100
    local amount = ga_bent_get_param_i(level, bp)
    if (amount == 0) then amount = default_amount end
    return amount
end

function p.payload(level, bp)
    if not game_can_use.main(9) then return end

    local amount = p.ammo(level, bp)

    local max_value = game_wep_modes.get_ammo_max(9)
    local old_value = ga_get_i("xar.player.gun9.ammo")
    local new_value = old_value + amount
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun9.ammo", new_value)
        if( amount >= 10 ) then
            ga_play_sound("oh_yeah")
        else
            ga_play_sound("pickup_ammo")
        end
        local msg2 = "You got " .. tostring(new_value - old_value) .. " nukes"
        game_msg.add(msg2)
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(9) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local ammo = p.ammo(level, bp)
    local core = tostring(ammo) .. " Nukes"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Ammo/bent_ammo_gun_9_once.lua"
function p.__get_mesh() return "bent_ammo_gun_9" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    max_value = game_wep_modes.get_ammo_max(9)
    old_value = ga_get_i("xar.player.gun9.ammo")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    if not game_can_use.main(9) then return end    
    local max_value = game_wep_modes.get_ammo_max(9)
    local old_value = ga_get_i("xar.player.gun9.ammo")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gun9.ammo", new_value)
        ga_play_sound("pickup_ammo")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got a nuke")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if not game_can_use.main(9) then
        return bent_ammo_gun_2_small.cannot_use_msg()
    end
    local core = "Nuke (does not respawn)"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_1000p.lua"
function p.has_effect(level, bp)
    local max_value = 10 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 10 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        --ga_play_sound("pickup_armor")
        ga_play_sound("oh_yeah")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got jade armor (1000%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "1000% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_1000p_once.lua"
function p.__get_mesh() return "bent_armor_1000p" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = 10 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 10 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        --ga_play_sound("pickup_armor")
        ga_play_sound("oh_yeah")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got jade armor (1000%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "1000% Armor (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_100p.lua"
function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got bronze armor (100%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "100% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_100p_once.lua"
function p.__get_mesh() return "bent_armor_100p" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got bronze armor (100%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "100% Armor (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_10p.lua"
function p.has_effect(level, bp)
    local max_value = 10*ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.payload(level, bp)
    local max_value = 10*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    local new_value = old_value + math.floor(max_value / 100) --How to cast?
    if ( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.armor.amount", new_value)
        ga_play_sound("pickup_armor_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got an extra 10% armor")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "10% Armor"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_150p.lua"
function p.has_effect(level, bp)
    local max_value = 1.5 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = math.floor( 1.5 * ga_get_i("xar.player.health.max") )
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got silver armor (150%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "150% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_150p_once.lua"
function p.__get_mesh() return "bent_armor_150p" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = 1.5 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = math.floor( 1.5 * ga_get_i("xar.player.health.max") )
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got silver armor (150%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "150% Armor (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_200p.lua"
function p.has_effect(level, bp)
    local max_value = 2 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 2 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got gold armor (200%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "200% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_200p_once.lua"
function p.__get_mesh() return "bent_armor_200p" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = 2 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 2 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got gold armor (200%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "200% Armor (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_5p.lua"
function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    --new_value = old_value + 100
    local new_value = old_value + math.floor(max_value / 20) --How to cast?
    if ( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.armor.amount", new_value)
        ga_play_sound("pickup_armor_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got 5% more armor")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "5% Armor"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_600p.lua"
function p.has_effect(level, bp)
    local max_value = 6 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 6 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got ivory armor (600%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "600% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Armor/bent_armor_600p_once.lua"
function p.__get_mesh() return "bent_armor_600p" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = 6 * ga_get_i("xar.player.health.max")
    local old_armor = ga_get_i("xar.player.armor.amount")
    return ( old_armor < max_value );
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    local max_value = 6 * ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.armor.amount")
    if ( old_value < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        ga_play_sound("pickup_armor")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got ivory armor (600%)")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "600% Armor (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Genesis/bent_genesis_marker_long.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Use to remove"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    game_bent.remove_perm(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Genesis/bent_genesis_marker_short.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Use to remove"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    game_bent.remove_perm(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_1.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "1 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_10.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 10
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "10 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_100.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 100
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "100 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_1000.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 1000
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_large")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return 2.0
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "1000 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_2.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 2
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "2 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_20.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "20 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_200.lua"
function p.payload(level, bp)
    max_value = ga_get_i("xar.player.gold.max")
    old_value = ga_get_i("xar.player.gold.amount")
    new_value = old_value + 200
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "200 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_3.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 3
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "3 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_30.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 30
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "30 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_300.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 300
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "300 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_5.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "5 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_50.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "50 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_500.lua"
function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 500
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "500 Gold"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_1.lua"
function p.__get_mesh() return "bent_gold_1" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 1
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "1 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_10.lua"
function p.__get_mesh() return "bent_gold_10" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 10
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "10 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_100.lua"
function p.__get_mesh() return "bent_gold_100" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 100
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "100 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_1000.lua"
function p.__get_mesh() return "bent_gold_1000" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 1000
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_large")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "1000 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_2.lua"
function p.__get_mesh() return "bent_gold_2" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 2
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "2 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_20.lua"
function p.__get_mesh() return "bent_gold_20" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 20
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "20 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_200.lua"
function p.__get_mesh() return "bent_gold_200" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 200
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "200 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_3.lua"
function p.__get_mesh() return "bent_gold_3" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    max_value = ga_get_i("xar.player.gold.max")
    old_value = ga_get_i("xar.player.gold.amount")
    new_value = old_value + 3
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "3 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_30.lua"
function p.__get_mesh() return "bent_gold_30" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 30
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "30 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_300.lua"
function p.__get_mesh() return "bent_gold_300" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 300
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "300 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_5.lua"
function p.__get_mesh() return "bent_gold_5" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 5
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "5 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_50.lua"
function p.__get_mesh() return "bent_gold_50" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 50
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_small")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "50 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Gold/bent_gold_once_500.lua"
function p.__get_mesh() return "bent_gold_500" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.gold.max")
    local old_value = ga_get_i("xar.player.gold.amount")
    local new_value = old_value + 500
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.gold.amount", new_value)
        ga_play_sound("pickup_gold_medium")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got " .. tostring(new_value - old_value) .. " gold")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    return "500 Gold (does not respawn)"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_100p.lua"
function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    if( old_value < max_value ) then
        ga_set_i("xar.player.health.amount", max_value)
        ga_play_sound("pickup_health_100p")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Your health is now up to 100%")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "To 100% Health"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_100p_armor_100p.lua"
function p.has_effect(level, bp)
    local max_health = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    if ( old_health < max_health ) then return true end
    if ( old_armor < max_health ) then return true end
    return false
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    local picking_up = false
    if( old_health < max_value ) then
        ga_set_i("xar.player.health.amount", max_value)
        picking_up = true
    end
    if( old_armor < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        picking_up = true
    end
    if picking_up then
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "To 100% Health + Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_10p.lua"
function p.has_effect(level, bp)
    local max_value = 2*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.payload(level, bp)
    local max_value = 2*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    local new_value = old_value + math.floor(max_value / 20) --How to cast?
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.health.amount", new_value)
        ga_play_sound("pickup_health_10p")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got an extra 10% health")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Extra 10% health"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_200p.lua"
function p.has_effect(level, bp)
    local max_value = 2*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local max_value = 2*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    if( old_value < max_value ) then
        ga_set_i("xar.player.health.amount", max_value)
        ga_play_sound("pickup_health_200p")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Your health is now up to 200%")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "To 200% Health"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_200p_armor_100p.lua"
function p.has_effect(level, bp)
    local max_health = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    if ( old_health < 2*max_health ) then return true end
    if ( old_armor < max_health ) then return true end
    return false
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    local picking_up = false
    if( old_health < 2*max_value ) then
        ga_set_i("xar.player.health.amount", 2*max_value)
        picking_up = true
    end
    if( old_armor < max_value ) then
        ga_set_i("xar.player.armor.amount", max_value)
        picking_up = true
    end
    if picking_up then
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Health up to 200% and armor up to 100%")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "To 200% Health, 100% Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_200p_armor_200p.lua"
function p.has_effect(level, bp)
    local max_health = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    if ( old_health < 2*max_health ) then return true end
    if ( old_armor < 2*max_health ) then return true end
    return false
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_health = ga_get_i("xar.player.health.amount")
    local old_armor = ga_get_i("xar.player.armor.amount")
    local picking_up = false
    if( old_health < 2*max_value ) then
        ga_set_i("xar.player.health.amount", 2*max_value)
        picking_up = true
    end
    if( old_armor < 2*max_value ) then
        ga_set_i("xar.player.armor.amount", 2*max_value)
        picking_up = true
    end
    if picking_up then
        ga_play_sound("pickup_armor")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Health and armor both up to 200%")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "To 200% Health + Armor"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_25p.lua"
function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    local new_value = math.floor( old_value + (max_value / 4) ) --How to cast?
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.health.amount", new_value)
        ga_play_sound("pickup_health_25p")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got 25% more health")
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "25% health"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_mega.lua"
function p.has_effect(level, bp)
    local max_value = 5*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local one_hundred_p = ga_get_i("xar.player.health.max")
    local max_value = 5*one_hundred_p
    local old_value = ga_get_i("xar.player.health.amount")
    local new_value = old_value + one_hundred_p
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.health.amount", new_value)
        ga_play_sound("pickup_health_mega")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("You got an extra 100% health")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Extra 100% health"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_health_mega_once.lua"
function p.__get_mesh() return "bent_health_mega" end
function p.__get_mesh2() return "gun_ammo_once" end

function p.has_effect(level, bp)
    local max_value = 5*ga_get_i("xar.player.health.max")
    local old_value = ga_get_i("xar.player.health.amount")
    return ( old_value < max_value );
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    local one_hundred_p = ga_get_i("xar.player.health.max")
    local max_value = 5*one_hundred_p
    local old_value = ga_get_i("xar.player.health.amount")
    local new_value = old_value + one_hundred_p
    if( new_value > max_value ) then new_value = max_value end
    if ( new_value > old_value ) then
        ga_set_i("xar.player.health.amount", new_value)
        ga_play_sound("pickup_health_mega")
        game_bent.remove_perm(level, bp)
        game_msg.add("You got an extra 100% health")
    end
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Extra 100% health (does not respawn)"
    if( p.has_effect(level, bp) ) then return "^xffff00" .. core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    ga_play_sound("xar_error")
    game_msg.add("You must touch this item")
end

function p.on_telekinesis(level, bp)
    p.__on_touch(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Health/bent_shield.lua"
function p.has_effect(level, bp)
    local max_value = ga_get_i("xar.player.shield.max")
    local old_value = ga_get_i("xar.player.shield.amount")
    return ( old_value < max_value )
end

function p.payload(level, bp)
    local max_value = ga_get_i("xar.player.shield.max")
    local old_value = ga_get_i("xar.player.shield.amount")
    if( old_value < max_value ) then
        ga_set_i("xar.player.shield.amount", max_value)
        ga_play_sound("pickup_shield")
        game_bent.remove_temp(level, bp, 60 * 60)
        game_msg.add("Your shield is recharged")
    end
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local core = "Shield"
    if( p.has_effect(level, bp) ) then return core
    else return "^xff0000" .. core end
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Key/bent_buy_blue_key.lua"
function p.get_price(level, bp)
    return ga_bent_get_param_i(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local price = p.get_price(level, bp)
    return "Buy A Blue Key for " .. tostring(price) .. " gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    local price = p.get_price(level, bp)
    ga_create_i("temp.xar.ent.buy_blue_key.price")
    ga_set_i(   "temp.xar.ent.buy_blue_key.price", price)
    ga_window_push("win_buy_blue_key")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Key/bent_key_dans_house.lua"
function p.payload(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    ga_set_f("xar.key_time.dans_house", num_sec)
    ga_play_sound("pickup_powerup")
    game_bent.remove_temp(level, bp, 60) --One minute.
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return tostring(num_sec) .. " Farmhouse Key"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Key/bent_key_green.lua"
function p.payload(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    local combo = ga_bent_get_param_s(level, bp)
    ga_set_f("xar.key_time.green", num_sec)
    ga_set_s("xar.key_time.green_combo", combo)
    ga_play_sound("pickup_powerup")
    game_bent.remove_temp(level, bp, 60) --One minute.
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    local combo = ga_bent_get_param_s(level, bp)
    return tostring(num_sec) .. " Sec Green Key (with combo " .. combo .. ")"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Key/bent_key_laser_genesis.lua"
function p.payload(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    ga_set_f("xar.key_time.laser_genesis", num_sec)
    ga_play_sound("pickup_powerup")
    game_bent.remove_temp(level, bp, 60) --One minute.
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return tostring(num_sec) .. " Second Laser Genesis Key"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Key/bent_key_yellow.lua"
function p.payload(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    ga_set_f("xar.key_time.yellow", num_sec)
    ga_play_sound("pickup_powerup")
    game_bent.remove_temp(level, bp, 60) --One minute.
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return tostring(num_sec) .. " Second Yellow Key"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_arcade_enter.lua"
function p.__get_can_use(level, bp)
    return false
end

function p.__get_use_msg(level, bp)
    if bent_arcade_reset_door.door_is_shut(level, bp) then
        return "^xff0000You are in the arcade"
    else
        return "Touch to enter arcade"
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.payload(level, bp)
    game_arcade.set_inside_arcade()
    
    local vcp = ga_bp_to_parent_vcp(bp)

    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( block_type == "XAR_ARCADE_DOOR_ENTER_SOLID" ) then
            --Removing the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_ENTER_EMPTY")
        end
        if( block_type == "XAR_ARCADE_DOOR_SHUT_EMPTY" ) then
            --Adding the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_SHUT_SOLID")
        end
        -- local bent_type = ga_bent_get_type(level, bp2)
        -- if( bent_type == "bent_arcade_enter" ) then
        --     --Removing the arcade box.
        --     ga_bent_remove_perm(level, bp2)
        -- end
    end end end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_arcade_exit.lua"
function p.__get_can_use(level, bp)
    return false
end

function p.__get_use_msg(level, bp)
    if not bent_arcade_reset_door.door_is_shut(level, bp) then
        return "Touch to exit arcade"
    else
        return "^xff0000You have left the arcade"
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.payload(level, bp)
    game_arcade.set_outside_arcade()

    local vcp = ga_bp_to_parent_vcp(bp)
    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( block_type == "XAR_ARCADE_DOOR_EXIT_SOLID" ) then
            --Removing the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_EXIT_EMPTY")
        end
        if( block_type == "XAR_ARCADE_DOOR_SHUT_EMPTY" ) then
            --Adding the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_SHUT_SOLID")
        end
    end end end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_arcade_reset_door.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if p.door_is_shut(level, bp) then
        return "Reset door"
    else
        return "Door is ready to go through"
    end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

--Slow.
--Could make faster by using a chunk variable.
function p.door_is_shut(level, bp)
    local vcp = ga_bp_to_parent_vcp(bp)
    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( block_type == "XAR_ARCADE_DOOR_SHUT_SOLID" ) then
            return true
        end
    end end end
    return false
end

--Opening the door (if it is shut).
function p.payload(level, bp)
    if not p.door_is_shut(level, bp) then return end

    ga_play_sound("use")
    
    local vcp = ga_bp_to_parent_vcp(bp)
    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( block_type == "XAR_ARCADE_DOOR_ENTER_EMPTY" ) then
            --Adding the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_ENTER_SOLID")
        end
        if( block_type == "XAR_ARCADE_DOOR_EXIT_EMPTY" ) then
            --Adding the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_EXIT_SOLID")
        end
        if( block_type == "XAR_ARCADE_DOOR_SHUT_SOLID" ) then
            --Removing the block.
            ga_block_change_perm(level, bp2, "XAR_ARCADE_DOOR_SHUT_EMPTY")
        end
    end end end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_black_hole_control_panel.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Black Hole Drive Control Panel"
end

function p.__on_use(level, bp)
    ga_window_push("win_black_hole_control_panel")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_bookmark.lua"
-------------------------------------------------
--              Xar Respawn Point
-------------------------------------------------
-- This is the xar version of the "respawn point"
-- that is found in the base package.

-- function p.__get_mesh() return "bent_bookmark" end

function p.has_current_bookmark()
    return ga_get_b("xar.world_pos.bookmark.has")
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Bookmark"
end

function p.__on_use(level, bp)
    if( p.has_current_bookmark() ) then
        ga_window_push("win_bookmark2")
    else
        ga_window_push("win_bookmark")
    end
end

function p.__on_use2(level, bp)
    if( p.has_current_bookmark() ) then
        game_msg.add("You already have a current bookmark")
        game_msg.add("Left click to deal with this")
        ga_play_sound("error")
    else
        local chunk_id = ga_bp_to_parent_chunk_id(level, bp)
        local lbp = ga_bp_to_lbp(bp)
    game_bookmark_tele.start_tele_from_bookmark(chunk_id, lbp)
    end
end

function p.__on_render(level, bp)
    local mesh_name = "bent_bookmark"
    if( p.has_current_bookmark() ) then
        mesh_name = "bent_bookmark_gray"
    end
    --
    --Creating the classic "pulsating" look.
    local cur_time = ga_get_game_time()
    scale = 1.0 + math.cos(6.0 * cur_time) * 0.1
    ga_render_matrix_scaled(scale, scale, scale);
    --
    ga_render_mesh(mesh_name)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_credits.lua"
function p.__get_mesh() return "bent_credits" end

function p.__on_use(level, bp)
    ga_set_f("xar.key_time.universe", 20.0)
    ga_window_push("win_thank_you")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_difficulty_change.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Change Game Difficulty"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_window_push("win_diff_change")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_env_var_open_door.lua"
function p.get_var_name(level, bp)
    return ga_bent_get_param_s(level, bp)
end

function p.get_can_open(level, bp)
    local var = p.get_var_name(level, bp)
    if not ga_exists(var) then return false end --Uh oh!
    return ga_get_b(var)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    --Saftey check.
    local var = p.get_var_name(level, bp)
    if not ga_exists(var) then
        return "*** Error: var not found: " .. var
    end
    --
    if p.get_can_open(level,bp) then
        return "^x00ff00Open Door"
    else
        return "^xff0000Door Is Locked"
    end
end

function p.__on_use(level, bp)
    if p.get_can_open(level,bp) then
        p.remove_door(level, bp)
    else
        ga_play_sound("error")
    end
end

function p.remove_door(level, bp)
    ga_play_sound("toll_door")

    local vcp = ga_bp_to_parent_vcp(bp)

    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( block_type == "XAR_DOOR" ) then
            --Removing the block.
            ga_block_change_perm(level, bp2, "XAR_EMPTY_BORING")
        end
        --
        local bent_type = ga_bent_get_type(level, bp2)
        if( bent_type == "bent_env_var_open_door" ) then
            --Removing the box used to open the door.
            ga_bent_remove_perm(level, bp2)
        end
    end end end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_icarus.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Enable / disable Icarus mode"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.payload(level, bp)
    ga_window_push("win_icarus")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_open_green_door.lua"
function p.get_required_combo(level, bp)
    return ga_bent_get_param_s(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local combo = p.get_required_combo(level, bp)
    return "Use Key With Combination " .. tostring(combo)
end

function p.__on_use(level, bp)
    p.remove_door(level, bp)
end

--Could put elsewhere.
function p.remove_door(level, bp)
    local required_combo = p.get_required_combo(level, bp)
    local player_combo = ga_get_s("xar.key_time.green_combo")
    if( player_combo ~= required_combo ) then
        ga_play_sound("error")
        return
    end
    --
    ga_play_sound("toll_door")
    local vcp = ga_bp_to_parent_vcp(bp)

    local remove_bents = {}
    remove_bents["bent_open_green_door"] = true

    local remove_bts = {}
    remove_bts["XAR_DOOR_GREEN"] = true
    remove_bts["XAR_DOOR_GREEN_GLASS"] = true

    for xi = 0,15 do
    for yi = 0,15 do
    for zi = 0,15 do
        local lbp = std.bp(xi,yi,zi)
        local bp2 = std.lbp_to_bp(vcp, lbp)
        --
        local block_type = ga_block_get(level, bp2)
        if( remove_bts[block_type] ) then
            --Removing the block.
            ga_block_change_perm(level, bp2, "XAR_EMPTY_BORING")
        end
        --
        local bent_type = ga_bent_get_type(level, bp2)
        if( remove_bents[bent_type] ) then
            --Removing the bent used to open the door.
            ga_bent_remove_perm(level, bp2)
        end
    end end end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_quasar_control_panel.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Quasar Drive Control Panel"
end

function p.__on_use(level, bp)
    ga_window_push("win_quasar_control_panel")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_respawn.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Respawn"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.payload(level, bp)
    ga_window_push("win_respawn")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_respawn_point.lua"
--File: bent_respawn_point.lua

-------------------------------------------------
--              Xar Respawn Point
-------------------------------------------------
-- This is the xar version of the "respawn point"
-- that is found in the base package.

function p.__get_mesh() return "" end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Respawn Point"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_window_push("win_base_respawn_point") --New way.
end

function p.__on_use2(level, bp)
    game_bookmark_tele.start_tele_from_respawn_point()
end

function p.__on_render(level, bp)
    local vcp = std.bp_to_parent_vcp(bp)
    local chunk_id = ga_vcp_to_chunk_id(level, vcp)
    local path = ga_chunk_id_to_path(chunk_id)
    --
    local respawn_path = ga_get_respawn_path()
    --
    local mesh_name = "bent_base_respawn_inactive"
    if( path == respawn_path ) then
        mesh_name = "bent_base_respawn_active"
    end
    --
    --Creating the classic "pulsating" look.
    local cur_time = ga_get_game_time()
    scale = 1.0 + math.cos(6.0 * cur_time) * 0.1
    ga_render_matrix_scaled(scale, scale, scale);
    --
    ga_render_mesh(mesh_name)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_respawn_point_invisible.lua"
--File: bent_respawn_point_invisible.lua

-------------------------------------------------
--         Xar Respawn Point Invisible
-------------------------------------------------
-- This is placed in the starting room so the player can
-- respawn there, but they cannot see it or activate it.

function p.__on_render(level, bp)
    --Not rendering anything.
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_unlock_stable_sing.lua"
function p.get_area_name(level, bp)
    return ga_bent_get_param_s(level, bp)
end

function p.get_var_name(level, bp)
    local area_name = p.get_area_name(level, bp)
    return "xar.door_unlocked." .. area_name 
end

function p.get_door_unlocked(level, bp)
    local var = p.get_var_name(level, bp)
    if not ga_exists(var) then return false end --Uh oh!
    return ga_get_b(var)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if p.get_door_unlocked(level, bp) then
        return "^xffff00Door Already Unlocked in Stable Singletons Room"
    else
        return "^x00ff00Unlock Door in Stable Singletons Room"
    end
end

function p.__on_use(level, bp)
    ga_create_s("temp.xar.ent.unlock_stable_sing.area")
    ga_set_s("temp.xar.ent.unlock_stable_sing.area",
        p.get_area_name(level, bp))
    --
    local already_unlocked = p.get_door_unlocked(level,bp)
    ga_create_b("temp.xar.ent.unlock_stable_sing.already_unlocked")
    ga_set_b("temp.xar.ent.unlock_stable_sing.already_unlocked",
        already_unlocked)
    --
    --Actually unlocking.
    if not already_unlocked then
        ga_play_sound_menu("harp")
        local var = p.get_var_name(level, bp)
        ga_set_b(var, true) --The door is now unlocked.
    else
        ga_play_sound_menu("use")
    end
    --
    ga_window_push("win_unlock_stable_sing")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Other/bent_xp.lua"
function p.get_num(level, bp)
    return ga_bent_get_param_i(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local num = p.get_num(level, bp)
    return "Get " .. tostring(num) .. " Experience Points"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end

function p.payload(level, bp)
    local num = p.get_num(level, bp)
    local allow_powerup = false
    game_xp.add(num, allow_powerup)
    game_bent.remove_perm(level, bp)
    ga_play_sound("pickup_upgrade")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Powerup/bent_damage_5x_stacking.lua"
function p.payload(level, bp)
    local old_time = ga_get_f("xar.damage_5x_stacking_time")
    local num_sec = ga_bent_get_param_i(level, bp)
    local new_time = old_time + num_sec
    ga_set_f("xar.damage_5x_stacking_time", new_time)
    ga_play_sound("pickup_powerup")
    ga_hud_msg("Your Damage Is Multiplied by 5!", 5.0)
    game_msg.add("Your damage is multiplied by 5!")
    game_bent.remove_temp(level, bp, 60 * 60) --One hour.
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return "5x damage for " .. tostring(num_sec) .. " seconds"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Powerup/bent_invun_stacking.lua"
function p.payload(level, bp)
    local old_time = ga_get_f("xar.invun_stacking_time")
    if( old_time < 0.0 ) then old_time = 0.0 end
    local num_sec = ga_bent_get_param_i(level, bp)
    local new_time = old_time + num_sec
    ga_set_f("xar.invun_stacking_time", new_time)
    ga_play_sound("pickup_powerup")
    ga_hud_msg("You Are Invulnerable!", 5.0)
    game_msg.add("You are invulnerable!")
    game_bent.remove_temp(level, bp, 60 * 60) --One hour.
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return "Invulnerability for " .. tostring(num_sec) .. " seconds"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Powerup/bent_xp_5x_stacking.lua"
function p.payload(level, bp)
    local old_time = ga_get_f("xar.xp_5x_stacking_time")
    local num_sec = ga_bent_get_param_i(level, bp)
    local new_time = old_time + num_sec
    ga_set_f("xar.xp_5x_stacking_time", new_time)
    ga_play_sound("pickup_powerup")
    ga_hud_msg("You Get 5 Times the Experience!", 5.0)
    game_msg.add("You get 5 times the experience!")
    game_bent.remove_temp(level, bp, 60 * 60) --One hour.
end

function p.__get_touch_dist()
    return ga_get_f("xar.normal_bent_touch_dist")
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_use_msg(level, bp)
    local num_sec = ga_bent_get_param_i(level, bp)
    return "5x experience for " .. tostring(num_sec) .. " seconds"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_black_market.lua"
function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_window_push("win_black_market")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_marker_1.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Markers (1 gold each)"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_marker.price")
    ga_set_i("temp.xar.ent.buy_marker.price", 1)
    ga_window_push("win_buy_marker")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_marker_2.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Markers (2 gold each)"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_marker.price")
    ga_set_i("temp.xar.ent.buy_marker.price", 2)
    ga_window_push("win_buy_marker")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_marker_3.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Markers (3 gold each)"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_marker.price")
    ga_set_i("temp.xar.ent.buy_marker.price", 3)
    ga_window_push("win_buy_marker")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_marker_4.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Markers (4 gold each)"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_marker.price")
    ga_set_i("temp.xar.ent.buy_marker.price", 4)
    ga_window_push("win_buy_marker")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_marker_5.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Markers (5 gold each)"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_marker.price")
    ga_set_i("temp.xar.ent.buy_marker.price", 5)
    ga_window_push("win_buy_marker")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_100.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 100 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 100)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_150.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 150 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 150)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_200.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 200 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 200)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_250.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 250 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 250)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_300.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 300 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 300)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_nuke_400.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Nuke for 400 gold"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_create_i("temp.xar.ent.buy_nuke.price")
    ga_set_i("temp.xar.ent.buy_nuke.price", 400)
    ga_window_push("win_buy_nuke")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_buy_station.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Buy Station"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    --ga_create_i("xar.ent.buy_marker.price")
    --ga_set_i("xar.ent.buy_marker.price", 1)
    ga_window_push("win_buy_station")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_cheap_common_markers.lua"
function p.already_using()
    return ga_get_b("xar.player.cheat_comon_markers") 
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if p.already_using() then
        return "Already using"
    else
        return "Enable Cheap Common Markers"
    end
end

function p.__on_use(level, bp)
    if p.already_using() then return end
    ga_play_sound_menu("use")
    ga_window_push("win_cheap_common_markers")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_pay_toll.lua"
function p.get_price(level, bp)
    return ga_bent_get_param_i(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local price = p.get_price(level, bp)
    local price_str = game_str.add_commas(price)
    return "Pay " .. price_str .. " Toll"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    --
    local price = p.get_price(level, bp)
    ga_create_i("temp.xar.ent.pay_toll.price")
    ga_set_i(   "temp.xar.ent.pay_toll.price", price)
    --
    ga_create_i("temp.xar.ent.pay_toll.level")
    ga_set_i(   "temp.xar.ent.pay_toll.level", level)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.x")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.x", bp.x)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.y")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.y", bp.y)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.z")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.z", bp.z)
    --
    --This way the change will only be applied to the current chunk
    bent_pay_toll.init_use_level_vars(0)
    --
    ga_window_push("win_pay_toll")
end

--Helper function.
--num == 0 means it only applies to the current chunk.
--num == 1 means it also applies to the parent chunk.
--num == 2 means it also applies to the grandparent chunk.
function p.init_use_level_vars(num)
    ga_create_b("temp.xar.ent.pay_toll.level_minus_1")
    ga_create_b("temp.xar.ent.pay_toll.level_minus_2")
    --
    ga_set_b("temp.xar.ent.pay_toll.level_minus_1", (num >= 1))
    ga_set_b("temp.xar.ent.pay_toll.level_minus_2", (num >= 2))
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_pay_toll_parent.lua"
function p.get_price(level, bp)
    return ga_bent_get_param_i(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local price = p.get_price(level, bp)
    return "Pay " .. tostring(price) .. " Toll"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    --
    local price = p.get_price(level, bp)
    ga_create_i("temp.xar.ent.pay_toll.price")
    ga_set_i(   "temp.xar.ent.pay_toll.price", price)
    --
    ga_create_i("temp.xar.ent.pay_toll.level")
    ga_set_i(   "temp.xar.ent.pay_toll.level", level)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.x")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.x", bp.x)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.y")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.y", bp.y)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.z")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.z", bp.z)
    --
    --This way the change will also take place
    --in the PARENT chunk.
    bent_pay_toll.init_use_level_vars(1)
    --
    ga_window_push("win_pay_toll")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_pay_toll_parent2.lua"
function p.get_price(level, bp)
    return ga_bent_get_param_i(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    local price = p.get_price(level, bp)
    return "Pay " .. tostring(price) .. " Toll"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    --
    local price = p.get_price(level, bp)
    ga_create_i("temp.xar.ent.pay_toll.price")
    ga_set_i(   "temp.xar.ent.pay_toll.price", price)
    --
    ga_create_i("temp.xar.ent.pay_toll.level")
    ga_set_i(   "temp.xar.ent.pay_toll.level", level)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.x")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.x", bp.x)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.y")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.y", bp.y)
    --
    ga_create_i("temp.xar.ent.pay_toll.bp.z")
    ga_set_i(   "temp.xar.ent.pay_toll.bp.z", bp.z)
    --
    --This way the change will also take place
    --in the PARENT and GRANDPARENT chunk.
    bent_pay_toll.init_use_level_vars(2)
    --
    ga_window_push("win_pay_toll")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_sell_station_common.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Common Sell Station"
end

-- Used for all 3 types of sell_stations.
function p.helper1(type)
    if (type == 0) then return ga_randf() end
    if (type == 1) then return 0.75 + 0.25*ga_randf() end
    if (type == 2) then return 0.9 + 0.1*ga_randf() end
end

-- Used for all 3 types of sell stations.
--Types: 0 = common, 1 = scarse, 2 = rare.
function p.helper2(type)
    local num = 0
    local c = std.vec(0.0, 0.0, 0.0)
    local c1 = ga_get_sys_v("menu.text_color")
    local c2 = {x=1.0, y=1.0, z=0.0}
    local c3 = {x=0.0, y=1.0, z=0.0}

    --Health.
    local f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(10,20)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(5,9)
        c = c2
    else
        num = ga_randi(2,4)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.health")
    ga_create_v("temp.xar.ent.sell_station.health_color")
    ga_set_i("temp.xar.ent.sell_station.health", num)
    ga_set_v("temp.xar.ent.sell_station.health_color", c)

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(40,100)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(20,39)
        c = c2
    else
        num = ga_randi(10,19)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun2")
    ga_create_v("temp.xar.ent.sell_station.gun2_color")
    ga_set_i("temp.xar.ent.sell_station.gun2", num)
    ga_set_v("temp.xar.ent.sell_station.gun2_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(20,50)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(10,19)
        c = c2
    else
        num = ga_randi(5,9)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun3")
    ga_create_v("temp.xar.ent.sell_station.gun3_color")
    ga_set_i("temp.xar.ent.sell_station.gun3", num)
    ga_set_v("temp.xar.ent.sell_station.gun3_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(20,50)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(10,19)
        c = c2
    else
        num = ga_randi(7,9)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun4")
    ga_create_v("temp.xar.ent.sell_station.gun4_color")
    ga_set_i("temp.xar.ent.sell_station.gun4", num)
    ga_set_v("temp.xar.ent.sell_station.gun4_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(300,1009) --If it is 1009, it will round down to 1000.
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(200,299)
        c = c2
    else
        num = ga_randi(100,199)
        c = c3
    end
    num = math.floor(num / 10) * 10 --Rounding down to nearest multiple of 10.
    ga_create_i("temp.xar.ent.sell_station.gun5")
    ga_create_v("temp.xar.ent.sell_station.gun5_color")
    ga_set_i("temp.xar.ent.sell_station.gun5", num)
    ga_set_v("temp.xar.ent.sell_station.gun5_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(100,200)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(80,99)
        c = c2
    else
        num = ga_randi(70,79)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun6")
    ga_create_v("temp.xar.ent.sell_station.gun6_color")
    ga_set_i("temp.xar.ent.sell_station.gun6", num)
    ga_set_v("temp.xar.ent.sell_station.gun6_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(20,40)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(15,19)
        c = c2
    else
        num = ga_randi(10,14)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun7")
    ga_create_v("temp.xar.ent.sell_station.gun7_color")
    ga_set_i("temp.xar.ent.sell_station.gun7", num)
    ga_set_v("temp.xar.ent.sell_station.gun7_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        num = ga_randi(1,14)
        c = c1
    elseif (f < 0.99) then
        num = ga_randi(15,19)
        c = c2
    else
        num = ga_randi(20,25)
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun8")
    ga_create_v("temp.xar.ent.sell_station.gun8_color")
    ga_set_i("temp.xar.ent.sell_station.gun8", num)
    ga_set_v("temp.xar.ent.sell_station.gun8_color", c);

    f = bent_sell_station_common.helper1(type)
    if (f < 0.9) then
        --num = ga_randi(1,39)
        num = ga_randi(1,24)
        c = c1
    elseif (f < 0.99) then
        --num = ga_randi(40,59)
        num = ga_randi(25,49)
        c = c2
    else
        --num = ga_randi(60,100)
        num = ga_randi(50,60) --50 is black market rate.
        c = c3
    end
    ga_create_i("temp.xar.ent.sell_station.gun9")
    ga_create_v("temp.xar.ent.sell_station.gun9_color")
    ga_set_i("temp.xar.ent.sell_station.gun9", num)
    ga_set_v("temp.xar.ent.sell_station.gun9_color", c);
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")

    local seed = ga_block_seed_pos(level, bp) --New way.
    ga_srand(seed)
    
    bent_sell_station_common.helper2(0)

    ga_window_push("win_sell_station")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_sell_station_rare.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rare Sell Station"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")

    local seed = ga_block_seed_pos(level, bp) --New way.
    ga_srand(seed)
    
    bent_sell_station_common.helper2(2)

    ga_window_push("win_sell_station")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_sell_station_scarce.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Scarce Sell Station"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    
    local seed = ga_block_seed_pos(level, bp) --New way.
    ga_srand(seed)
    
    bent_sell_station_common.helper2(1)

    ga_window_push("win_sell_station")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_sleep.lua"
function p.__get_can_use(level, bp)
    local dist = ga_block_dist_to_viewer(level, bp)
    if( dist < 1.0 ) then return false end
    return true
end

function p.__get_use_msg(level, bp)
    if( not p.__get_can_use(level, bp) ) then return "" end
    return "Sleep (choose a duration)"
end

function p.__on_use()
    ga_play_sound_menu("use")
    ga_window_push("win_sleep")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_sleep_hour.lua"
function p.__get_mesh() return "bent_sleep" end

function p.__get_can_use(level, bp)
    local dist = ga_block_dist_to_viewer(level, bp)
    if( dist < 1.0 ) then return false end
    return true
end

function p.__get_use_msg(level, bp)
    if( not p.__get_can_use(level, bp) ) then return "" end
    return "Pay 300 to Sleep for an Hour"
end

function p.__on_use()
    ga_play_sound_menu("use")
    win_sleep.payload(60 * 60)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_upgrade_station.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Upgrade Station"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    ga_window_push("win_upgrade_station")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_upgrade_station_max_ammo.lua"
function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Max Ammo Upgrade Station"
end

function p.__on_use(level, bp)
    ga_play_sound_menu("use")
    local chunk_id = ga_bp_to_parent_chunk_id(level, bp)
    ga_create_i("temp.xar.upgrade_station_max_ammo.chunk_id")
    ga_set_i("temp.xar.upgrade_station_max_ammo.chunk_id", chunk_id)
    ga_window_push("win_upgrade_station_max_ammo")
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Store/bent_upgrade_telekinesis.lua"
--File: bent_upgrade_telekinesis.lua

function p.upgrade_name(level, bp)
    return ga_bent_get_param_s(level, bp)
end

function p.get_var_name(level, bp)
    local name = p.upgrade_name(level, bp)
    return "game.globals.xar.player.telekinesis." .. name
end

function p.has_upgrade(level, bp)
    return ga_get_b( p.get_var_name(level, bp) )
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    if( p.has_upgrade(level,bp) ) then
        return "^xffff00View Telekinesis Upgrades"
    else
        return "Upgrade Telekinesis"
    end    
end

function p.__on_use(level, bp)
    if( p.has_upgrade(level,bp) ) then
        --Displaying the window.
        ga_play_sound_menu("use")
        ga_window_push("win_upgrade_telekinesis")
    else
        --Upgrading the telekinesis ability.
        local var = p.get_var_name(level, bp)
        ga_set_b(var, true)
        ga_play_sound("oh_yeah")
    end
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Trophy/bent_secret_trophy.lua"
function p.__get_mesh()
    return "secret_trophy"
end

function p.payload(level, bp)
    local trophy_name = ga_bent_get_param_s(level, bp)
    game_secret_trophy.check_secret_trophy(trophy_name)
    game_trophy.pickup_secret_trophy(trophy_name)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Secret Trophy"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_armor_regen.lua"
function p.__get_mesh() return "bent_armor_1000p" end
function p.__get_mesh2() return "armor_incr_regen" end

function p.payload(level, bp)
    game_upgrades.armor_regen()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Armor Regeneration"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_0_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_0" end
function p.__get_mesh2() return "gun_0_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.dark_hole_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Dark Hole Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_0_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_0" end
function p.__get_mesh2() return "gun_0_incr_damage" end

function p.payload(level, bp)
    game_upgrades.dark_hole_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Dark Hole Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_0_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_0" end
function p.__get_mesh2() return "gun_0_incr_proj_speed" end

function p.payload(level, bp)
    game_upgrades.dark_hole_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Dark Hole Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_1_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_1" end
function p.__get_mesh2() return "gun_1_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.plasma_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Plasma Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_1_ammo_regen.lua"
function p.__get_mesh() return "bent_ammo_gun_1" end
function p.__get_mesh2() return "gun_1_incr_ammo_regen" end

function p.payload(level, bp)
    game_upgrades.plasma_regen()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Plasma Ammo Regeneration"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_1_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_1" end
function p.__get_mesh2() return "gun_1_incr_damage" end

function p.payload(level, bp)
    game_upgrades.plasma_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Plasma Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_1_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_1" end
function p.__get_mesh2() return "gun_1_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.plasma_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Plasma Fire Rate"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_1_proj_speed.lua"
--Using a custom render function instead.
-- function p.__get_mesh() return "bent_ammo_gun_1" end
-- function p.__get_mesh2() return "gun_1_incr_proj_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.plasma_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Plasma Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end

--Testing.
function p.__on_render(level, bp)
    --Creating the classic "pulsating" look.
    --Could put in a helper function.
    local cur_time = ga_get_game_time()
    scale = 1.0 + math.cos(6.0 * cur_time) * 0.1
    ga_render_matrix_scaled(scale, scale, scale);
    --
    local mesh = "bent_ammo_gun_1"
    local tex = ga_mesh_get_tex(mesh)
    -- ga_render_mesh_with_tex_no_lighting(mesh, tex)
    -- ga_render_mesh_with_tex(mesh, tex)
    ga_render_mesh(mesh)
    --
    local mesh = "gun_1_incr_proj_speed"
    local tex = ga_mesh_get_tex(mesh)
    -- ga_render_mesh_with_tex_no_lighting(mesh, tex)
    -- ga_render_mesh_with_tex(mesh, tex)
    ga_render_mesh(mesh)
    --
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_2_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_2_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.cannon_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Cannon Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_2_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_2_incr_damage" end

function p.payload(level, bp)
    game_upgrades.cannon_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Cannon Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_2_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_2_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.cannon_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Cannon Fire Rate"
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_2_num_shots.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_2_incr_num_shots" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.cannon_num()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Cannon Num Shots"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_2_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_2_large" end
function p.__get_mesh2() return "gun_2_incr_proj_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.cannon_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Cannon Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_3_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_3_large" end
function p.__get_mesh2() return "gun_3_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.laser_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Laser Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_3_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_3_large" end
function p.__get_mesh2() return "gun_3_incr_damage" end

function p.payload(level, bp)
    game_upgrades.laser_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Laser Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_3_num.lua"
function p.__get_mesh() return "bent_ammo_gun_3_large" end
function p.__get_mesh2() return "gun_3_incr_num" end

function p.payload(level, bp)
    game_upgrades.laser_super()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Laser Special Damage Modifier"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_3_time.lua"
function p.__get_mesh() return "bent_ammo_gun_3_large" end
function p.__get_mesh2() return "gun_3_incr_time" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.laser_freeze_time()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Laser Freeze Time"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_4_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_4_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.rocket_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rocket Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_4_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_4_incr_damage" end

function p.payload(level, bp)
    game_upgrades.rocket_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rocket Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_4_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_4_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.rocket_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rocket Fire Rate"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_4_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_4_incr_proj_speed" end

function p.payload(level, bp)
    game_upgrades.rocket_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rocket Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_4_radius.lua"
function p.__get_mesh() return "bent_ammo_gun_4_large" end
function p.__get_mesh2() return "gun_4_incr_radius" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.rocket_radius()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Rocket Radius"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_5_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_5_large" end
function p.__get_mesh2() return "gun_5_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.homing_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Homing Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_5_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_5_large" end
function p.__get_mesh2() return "gun_5_incr_damage" end

function p.payload(level, bp)
    game_upgrades.homing_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Homing Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end

END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_5_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_5_large" end
function p.__get_mesh2() return "gun_5_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.homing_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Homing Fire Rate"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_5_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_5_large" end
function p.__get_mesh2() return "gun_5_incr_proj_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.homing_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Homing Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_6_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_6_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.minigun_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Minigun Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_6_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_6_incr_damage" end

function p.payload(level, bp)
    game_upgrades.minigun_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Minigun Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_6_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_6_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.minigun_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Minigun Fire Rate"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_6_num.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_6_incr_num" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.minigun_super()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Minigun Special Damage Modifier"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_6_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_6_large" end
function p.__get_mesh2() return "gun_6_incr_proj_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.minigun_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Minigun Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_7_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_7_large" end
function p.__get_mesh2() return "gun_7_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.railgun_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Railgun Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_7_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_7_large" end
function p.__get_mesh2() return "gun_7_incr_damage" end

function p.payload(level, bp)
    game_upgrades.railgun_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Railgun Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_7_fire_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_7_large" end
function p.__get_mesh2() return "gun_7_incr_fire_speed" end

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    game_upgrades.railgun_fire_rate()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end


function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Railgun Fire Rate"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_8_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_8" end
function p.__get_mesh2() return "gun_8_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.emp_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "EMP Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_8_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_8" end
function p.__get_mesh2() return "gun_8_incr_damage" end

function p.payload(level, bp)
    game_upgrades.emp_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "EMP Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_8_radius.lua"
function p.__get_mesh() return "bent_ammo_gun_8" end
function p.__get_mesh2() return "gun_8_incr_radius" end

function p.payload(level, bp)
    game_upgrades.emp_radius()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "EMP Radius"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_8_time.lua"
function p.__get_mesh() return "bent_ammo_gun_8" end
function p.__get_mesh2() return "gun_8_incr_num" end

function p.payload(level, bp)
    game_upgrades.emp_freeze_time()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "EMP Freeze Time"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_9_ammo_cap.lua"
function p.__get_mesh() return "bent_ammo_gun_9" end
function p.__get_mesh2() return "gun_9_incr_capacity" end

function p.payload(level, bp)
    game_upgrades.nuke_max_ammo()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Nuke Max Ammo"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_9_damage.lua"
function p.__get_mesh() return "bent_ammo_gun_9" end
function p.__get_mesh2() return "gun_9_incr_damage" end

function p.payload(level, bp)
    game_upgrades.nuke_damage()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Nuke Damage"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_9_proj_speed.lua"
function p.__get_mesh() return "bent_ammo_gun_9" end
function p.__get_mesh2() return "gun_9_incr_proj_speed" end

function p.payload(level, bp)
    game_upgrades.nuke_proj_speed()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Nuke Projectile Speed"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_gun_9_radius.lua"
function p.__get_mesh() return "bent_ammo_gun_9" end
function p.__get_mesh2() return "gun_9_incr_radius" end

function p.payload(level, bp)
    game_upgrades.nuke_radius()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Nuke Radius"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_health_cap.lua"
function p.__get_mesh() return "bent_health_100p" end
function p.__get_mesh2() return "health_incr" end

function p.get(level, bp)
    game_upgrades.health_max()
    game_bent.remove_perm(level, bp)
end

function p.payload(level, bp)
    p.get(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Max Health Upgrade"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_health_regen.lua"
function p.__get_mesh() return "bent_health_100p" end
function p.__get_mesh2() return "health_incr_regen" end

function p.get(level, bp)
    game_upgrades.health_regen()
    game_bent.remove_perm(level, bp)
end

function p.payload(level, bp)
    p.get(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Health Regeneration Upgrade"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_shield.lua"
function p.__get_mesh() return "shield_large" end --Antiquated name.
function p.__get_mesh2() return "shield_incr_ballance" end --Antiquated name.

function p.payload(level, bp)
    ga_srand(ga_block_seed_pos(level, bp))
    local i = ga_randi(0,1)
    if (i == 0) then bent_upgrade_health_cap.get(level, bp) end
    if (i == 1) then bent_upgrade_health_regen.get(level, bp) end
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Random Health Upgrade"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/Upgrade/bent_upgrade_spice.lua"
function p.__get_mesh() return "bent_spice" end
function p.__get_mesh2() return "health_incr_regen" end

function p.payload(level, bp)
    game_upgrades.spice()
    game_bent.remove_perm(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "RejuvenX";
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_cannon_2.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(2,2)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Tommy Gun"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_cannon_4.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(2,4)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: The Sheriff"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_cannon_5.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(2,5)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: The Judge"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_dark_hole_3.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(0,3)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Black Hole Drive"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_dark_hole_4.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(0,4)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Quasar Drive"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_laser_2.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(3,2)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Cold Beam"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_laser_3.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(3,3)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Freeze Ray"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_nuke_3.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(9,3)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Nuclear Pulse Propulsion"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_plasma_3.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(1,3)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Plasma Shotgun"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_plasma_4.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(1,4)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Plasma Hyper Rapid Fire"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_plasma_5.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(1,5)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Plasma Lightning Rifle"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_plasma_6.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(1,6)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Plasma Flare"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_railgun_2.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(7,2)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Rapid Railgun"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_railgun_4.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(7,4)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Super Vampire"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_rocket_2.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(4,2)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: High Velocity Rockets"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_rocket_3.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(4,3)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Big Radius Rockets"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
BEGIN_FILE: "../../Data/Packages/xar/BasicEnts/WeaponMods/bent_weapon_mod_rocket_5.lua"
function p.__get_mesh()
    return "weapon_mode"
end

function p.payload(level, bp)
    game_wep_modes.give_mode(4,5)
    game_bent.remove_perm(level, bp)
end

function p.__get_touch_dist()
    local dist = ga_get_f("xar.normal_bent_touch_dist")
    return dist
end

function p.__get_can_use(level, bp)
    return true
end

function p.__get_use_msg(level, bp)
    return "Weapon Mod: Progressive Rockets"
end

function p.__on_use(level, bp)
    p.payload(level, bp)
end

function p.__on_touch(level, bp)
    p.payload(level, bp)
end

function p.on_telekinesis(level, bp)
    p.payload(level, bp)
end
END_FILE
